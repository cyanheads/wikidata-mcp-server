---
summary: "Metadata alignment to pubmed gold standard — scripts, descriptions, Dockerfile labels, README badges"
breaking: false
security: false
---

# 0.1.4 — 2026-05-23

## Changed

- **`package.json` scripts** — all script runners changed from `tsx` to `bun run`; `start` alias removed; `format` target dropped trailing `.`; `test` updated to `bunx vitest run`; `start:stdio` and `start:http` updated to `bun ./dist/index.js`.
- **`package.json` fields** — `keywords` adds `"ai-agent"`; `engines.bun` tightened to `>=1.3.2`; `files` array drops `changelog/` and `AGENTS.md`.
- **`Dockerfile` LABEL** — `org.opencontainers.image.description` extended to include "via MCP. STDIO or Streamable HTTP." to match other description surfaces.
- **`manifest.json` description** — aligned to match the full tagline including transport suffix.
- **README badge row** — Docker badge added; Bun badge version updated to `v1.3.2`.
- **`devcheck.config.json`** — `tsx` added to depcheck ignores (still used in script shebangs; invoked via `bun run`).
